version: "3.9"

services:
  db:
    image: postgres:16
    restart: always
    environment:
      - POSTGRES_USER=moraki_app
      - POSTGRES_PASSWORD=moraki_app
      - POSTGRES_DB=moraki_development
    ports:
      - '5432:5432'